// Package baseline provides functionality for saving and comparing
// an approved port baseline for each monitored host. Operators can
// capture the current open-port state as a "known-good" baseline and
// receive alerts when live scans deviate from it.
package baseline
