// Package envelope provides the Envelope type — a self-contained unit
// that bundles a host's scan results with contextual metadata (tags,
// labels, scan time, and a unique scan ID) for use by downstream
// pipeline stages such as notifiers, reporters, and auditors.
package envelope
